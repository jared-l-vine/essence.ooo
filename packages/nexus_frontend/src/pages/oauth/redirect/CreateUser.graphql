mutation CreateUser(
  $id: Int!
  $username: String!
  $discriminator: Int!
  $avatar: String
) {
  insert_users(
    objects: {
      id: $id
      username: $username
      discriminator: $discriminator
      avatar: $avatar
    }
    on_conflict: {
      constraint: users_pkey
      update_columns: [avatar, discriminator, username]
    }
  ) {
    returning {
      id
    }
  }
}
